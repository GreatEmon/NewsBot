import PropTypes from 'prop-types'
import React, { Component } from 'react';
import loading from "../../public/loadin"

export default class Spinner extends Component {
  static propTypes = {second: third}

  render() {
    return (
      <div>Spinner</div>
    )
  }
}
